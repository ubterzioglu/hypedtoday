# Plan: LinkedIn Link Update + Turkish Localization (i18n)

## Overview
Two tasks:
1. **Update LinkedIn link** on homepage CTA from `https://www.linkedin.com/groups/16927008/` → `https://www.linkedin.com/company/hyped-today/`
2. **Add Turkish language support** using `react-i18next` with full translation coverage

---

## Step 1: Update LinkedIn Company Page Link

**Files to change:**
- `src/pages/Index.tsx` (line 115) — Change `href` from `https://www.linkedin.com/groups/16927008/` to `https://www.linkedin.com/company/hyped-today/`
- `src/components/Footer.tsx` (line 15) — Update LinkedIn social link to `https://www.linkedin.com/company/hyped-today/`

---

## Step 2: Install i18n Dependencies

```bash
npm install i18next react-i18next i18next-browser-languagedetector
```

- `i18next` — Core i18n framework
- `react-i18next` — React bindings (useTranslation hook)
- `i18next-browser-languagedetector` — Auto-detect user language from browser settings / localStorage

---

## Step 3: Create Translation Files

Create two JSON files with all ~230 translatable strings:

- `src/i18n/locales/en.json` — English (copy of current hardcoded strings)
- `src/i18n/locales/tr.json` — Turkish translations

### Translation Key Structure

```json
{
  "common": {
    "appName": "HYPED",
    "tagline": "Innovate. Connect. Create. Promote.",
    "anonymous": "Anonymous",
    "loading": "Loading..."
  },
  "nav": {
    "home": "Home",
    "explore": "Explore",
    "addProject": "Add Project",
    "leaderboard": "Leaderboard",
    "howItWorks": "How It Works",
    "contact": "Contact",
    "signIn": "Sign In",
    "signOut": "Sign Out",
    "myClaims": "My Claims",
    "myReviews": "My Reviews",
    "admin": "Admin"
  },
  "home": {
    "welcomeTo": "Welcome to",
    "addYourProject": "Add Your Project",
    "platformStats": "Platform Stats",
    "quickActions": "Quick Actions",
    "topProjects": "Top Projects",
    "viewAll": "View All",
    "joinCommunity": "Join Our Community!",
    "joinDescription": "Connect with other creators...",
    "joinLinkedInGroup": "Join LinkedIn Group 🚀"
  },
  "quickActions": {
    "addProject": "Add Project",
    "addProjectDesc": "Showcase your work",
    "explore": "Explore",
    "exploreDesc": "Discover projects",
    "vote": "Vote",
    "voteDesc": "Rate & support",
    "leaderboard": "Leaderboard",
    "leaderboardDesc": "Top ranked",
    "howItWorks": "How It Works",
    "howItWorksDesc": "Learn more",
    "contact": "Contact",
    "contactDesc": "Get in touch"
  },
  "stats": {
    "activePosts": "Active Posts",
    "supporters": "Supporters",
    "approvedSupports": "Approved Supports",
    "totalPoints": "Total Points"
  },
  "showroom": { ... },
  "leaderboard": { ... },
  "contact": { ... },
  "howItWorks": { ... },
  "auth": { ... },
  "admin": { ... },
  "ownerReviews": { ... },
  "supporterDashboard": { ... },
  "projectCard": { ... },
  "projectSubmission": { ... },
  "requestCapacity": { ... },
  "notFound": { ... },
  "footer": { ... },
  "banner": { ... }
}
```

---

## Step 4: Create i18n Configuration

Create `src/i18n/index.ts`:

```ts
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';
import LanguageDetector from 'i18next-browser-languagedetector';
import en from './locales/en.json';
import tr from './locales/tr.json';

i18n
  .use(LanguageDetector)
  .use(initReactI18next)
  .init({
    resources: { en: { translation: en }, tr: { translation: tr } },
    fallbackLng: 'en',
    interpolation: { escapeValue: false },
    detection: {
      order: ['localStorage', 'navigator'],
      caches: ['localStorage'],
    },
  });

export default i18n;
```

Import in `src/main.tsx` (add `import './i18n';` before App render).

---

## Step 5: Add Language Switcher Component

Create `src/components/LanguageSwitcher.tsx`:

- A small dropdown or toggle button (TR | EN) in the Header
- Uses `i18next.changeLanguage()` 
- Persists selection via localStorage (handled by detector)

---

## Step 6: Replace Hardcoded Strings

**All 20 files** need their hardcoded strings replaced with `t('key')` calls. Files in order:

### Priority 1 — Core layout (used on every page)
1. `src/components/Header.tsx` — nav items, auth buttons, banner
2. `src/components/Footer.tsx` — nav links, social labels, tagline

### Priority 2 — Homepage
3. `src/pages/Index.tsx` — hero, stats heading, CTA section
4. `src/components/DashboardStats.tsx` — stat labels
5. `src/components/QuickActions.tsx` — action labels & descriptions
6. `src/components/TopProjects.tsx` — empty states, labels

### Priority 3 — Core pages
7. `src/pages/Showroom.tsx` — heading, empty states
8. `src/pages/AddProject.tsx` — heading, info text
9. `src/pages/Leaderboard.tsx` — heading, table headers
10. `src/pages/Contact.tsx` — heading, labels, placeholders, toasts
11. `src/pages/HowItWorks.tsx` — steps, heading, CTA
12. `src/pages/AdminLogin.tsx` — heading, form labels, toasts

### Priority 4 — Dashboard pages
13. `src/pages/AdminDashboard.tsx` — tabs, stat labels
14. `src/pages/OwnerReviews.tsx` — headings, labels, buttons
15. `src/pages/SupporterDashboard.tsx` — headings, labels, buttons
16. `src/pages/ProjectComments.tsx` — headings, labels

### Priority 5 — Reusable components
17. `src/components/ProjectCard.tsx` — task labels, toasts, buttons
18. `src/components/ProjectSubmissionForm.tsx` — form labels, validation, toasts
19. `src/components/RequestCapacityWidget.tsx` — status labels
20. `src/components/LeaderboardItem.tsx` — labels
21. `src/components/FeaturedCarousel.tsx` — fallback strings
22. `src/pages/NotFound.tsx` — heading, button

### Pattern for replacement:

```tsx
// Before:
<h1>Join Our Community!</h1>

// After:
const { t } = useTranslation();
<h1>{t('home.joinCommunity')}</h1>
```

For toast messages:
```tsx
// Before:
toast.error("Please enter a message!");

// After:
toast.error(t('contact.errorEmptyMessage'));
```

---

## Step 7: Verification

1. Run `npm run build` — ensure no TypeScript errors
2. Run `npm run dev` — manual check:
   - Default language (EN) works identically to before
   - Language switcher toggles to Turkish
   - All text switches to Turkish
   - LinkedIn link points to `https://www.linkedin.com/company/hyped-today/`
3. Run linting if available

---

## Estimated Scope

| Task | Files Changed |
|------|---------------|
| LinkedIn link update | 2 files |
| i18n setup + config | 3 new files |
| Translation files | 2 new files (en.json, tr.json) |
| String replacement | ~20 existing files |
| Language switcher | 1 new component |
| **Total** | ~28 file operations |

---

## Notes

- Brand name "HYPED" and "supporttopromote" stay in English (no translation)
- The Turkish banner text in Header.tsx line 31 will move into translation files (en version can be the same or a translation)
- Admin dashboard strings can be kept in English initially if preferred (admin-only)
- `HeroSection.tsx` exists but is NOT used on Index.tsx (it has its own inline hero) — translate it anyway for consistency
